# OTIMIZAÇÃO STARNEW INFORMÁTICA - .htaccess
# Configurações otimizadas para performance e cache

# Redirecionar HTTP para HTTPS
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# OTIMIZAÇÃO: Headers de Segurança
Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
Header always set X-Content-Type-Options nosniff
Header always set X-Frame-Options DENY
Header always set X-XSS-Protection "1; mode=block"
Header always set Referrer-Policy "strict-origin-when-cross-origin"
Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"

# OTIMIZAÇÃO: Content Security Policy melhorada
Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.tailwindcss.com https://fonts.googleapis.com https://formspree.io; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https:; connect-src 'self' https://formspree.io; frame-src https://www.google.com;"

# OTIMIZAÇÃO: Compressão Gzip/Brotli
<IfModule mod_deflate.c>
    # Habilitar compressão para tipos de arquivo específicos
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE text/javascript
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
    AddOutputFilterByType DEFLATE application/json
    AddOutputFilterByType DEFLATE image/svg+xml
    AddOutputFilterByType DEFLATE font/woff
    AddOutputFilterByType DEFLATE font/woff2
    AddOutputFilterByType DEFLATE application/font-woff
    AddOutputFilterByType DEFLATE application/font-woff2
    
    # Configurações avançadas de compressão
    DeflateCompressionLevel 6
    DeflateMemLevel 9
    DeflateWindowSize 15
</IfModule>

# OTIMIZAÇÃO: Compressão Brotli (se disponível)
<IfModule mod_brotli.c>
    BrotliCompressionQuality 6
    BrotliFilterNote Input instream
    BrotliFilterNote Output outstream
    BrotliFilterNote Ratio ratio
    
    AddOutputFilterByType BROTLI_COMPRESS text/html text/plain text/xml text/css text/javascript application/javascript application/json image/svg+xml font/woff font/woff2
</IfModule>

# OTIMIZAÇÃO: Cache de Navegador Agressivo
<IfModule mod_expires.c>
    ExpiresActive on
    
    # Imagens - Cache por 1 ano
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/avif "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType image/x-icon "access plus 1 year"
    ExpiresByType image/vnd.microsoft.icon "access plus 1 year"
    
    # CSS e JavaScript - Cache por 1 ano
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    ExpiresByType text/javascript "access plus 1 year"
    ExpiresByType application/x-javascript "access plus 1 year"
    
    # Fontes - Cache por 1 ano
    ExpiresByType font/woff "access plus 1 year"
    ExpiresByType font/woff2 "access plus 1 year"
    ExpiresByType application/font-woff "access plus 1 year"
    ExpiresByType application/font-woff2 "access plus 1 year"
    ExpiresByType font/truetype "access plus 1 year"
    ExpiresByType font/opentype "access plus 1 year"
    ExpiresByType application/vnd.ms-fontobject "access plus 1 year"
    
    # Documentos e outros recursos
    ExpiresByType application/pdf "access plus 1 month"
    ExpiresByType application/xml "access plus 1 hour"
    ExpiresByType application/json "access plus 1 hour"
    
    # HTML - Cache mais curto para permitir atualizações
    ExpiresByType text/html "access plus 1 hour"
    
    # Manifests e service workers
    ExpiresByType application/manifest+json "access plus 1 week"
    ExpiresByType text/cache-manifest "access plus 0 seconds"
</IfModule>

# OTIMIZAÇÃO: Headers de Cache-Control
<IfModule mod_headers.c>
    # Cache agressivo para recursos estáticos
    <FilesMatch "\.(css|js|png|jpg|jpeg|gif|webp|avif|svg|woff|woff2|ttf|otf|eot)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
        Header set Vary "Accept-Encoding"
        Header unset ETag
        FileETag None
    </FilesMatch>
    
    # Cache para HTML com validação
    <FilesMatch "\.(html|htm)$">
        Header set Cache-Control "public, max-age=3600, must-revalidate"
        Header set Vary "Accept-Encoding"
    </FilesMatch>
    
    # Cache para documentos
    <FilesMatch "\.(pdf|doc|docx|xls|xlsx|ppt|pptx)$">
        Header set Cache-Control "public, max-age=2592000"
        Header set Vary "Accept-Encoding"
    </FilesMatch>
    
    # Headers para WebP
    <FilesMatch "\.webp$">
        Header set Cache-Control "public, max-age=31536000, immutable"
        Header set Vary "Accept"
    </FilesMatch>
</IfModule>

# OTIMIZAÇÃO: Preload de recursos críticos
<IfModule mod_headers.c>
    # Preload de fontes críticas
    <FilesMatch "\.html$">
        Header add Link "</fonts/inter-v12-latin-regular.woff2>; rel=preload; as=font; type=font/woff2; crossorigin"
        Header add Link "</images/logo.webp>; rel=preload; as=image; type=image/webp"
    </FilesMatch>
</IfModule>

# OTIMIZAÇÃO: Configurações de ETags
<IfModule mod_headers.c>
    # Remover ETags para recursos com cache longo
    <FilesMatch "\.(css|js|png|jpg|jpeg|gif|webp|svg|woff|woff2)$">
        Header unset ETag
        FileETag None
    </FilesMatch>
</IfModule>

# OTIMIZAÇÃO: Proteção contra acesso a arquivos sensíveis
<Files ~ "^\.">
    Order allow,deny
    Deny from all
</Files>

<Files ~ "\.(htaccess|htpasswd|ini|log|sh|sql|conf|bak|backup|old)$">
    Order allow,deny
    Deny from all
</Files>

# OTIMIZAÇÃO: Prevenir listagem de diretórios
Options -Indexes

# OTIMIZAÇÃO: Configurações de MIME types para WebP e outros formatos modernos
<IfModule mod_mime.c>
    AddType image/webp .webp
    AddType image/avif .avif
    AddType font/woff .woff
    AddType font/woff2 .woff2
    AddType application/font-woff .woff
    AddType application/font-woff2 .woff2
</IfModule>

# OTIMIZAÇÃO: Configurações de Keep-Alive
<IfModule mod_headers.c>
    Header set Connection keep-alive
</IfModule>

# OTIMIZAÇÃO: URL Rewriting para URLs amigáveis
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^([^\.]+)$ $1.html [NC,L]

# OTIMIZAÇÃO: Configurações de servidor para performance
<IfModule mod_deflate.c>
    # Não comprimir imagens já comprimidas
    SetEnvIfNoCase Request_URI \
        \.(?:gif|jpe?g|png|webp|avif)$ no-gzip dont-vary
    SetEnvIfNoCase Request_URI \
        \.(?:exe|t?gz|zip|bz2|sit|rar)$ no-gzip dont-vary
</IfModule>

# OTIMIZAÇÃO: Headers para melhor performance de CDN
<IfModule mod_headers.c>
    # Headers para melhor cache de CDN
    <FilesMatch "\.(css|js|png|jpg|jpeg|gif|webp|svg|woff|woff2)$">
        Header set X-Content-Type-Options nosniff
        Header set X-Frame-Options DENY
    </FilesMatch>
</IfModule>

# OTIMIZAÇÃO: Configurações específicas para imagens
<IfModule mod_headers.c>
    # Headers específicos para imagens WebP
    <FilesMatch "\.webp$">
        Header set Accept-Ranges bytes
        Header set Content-Type image/webp
    </FilesMatch>
    
    # Headers para imagens responsivas
    <FilesMatch "\.(png|jpg|jpeg|gif|webp|svg)$">
        Header set Accept-Ranges bytes
        Header append Vary Accept
    </FilesMatch>
</IfModule>

# OTIMIZAÇÃO: Configurações de timeout e limites
<IfModule mod_reqtimeout.c>
    RequestReadTimeout header=20-40,MinRate=500 body=20,MinRate=500
</IfModule>

